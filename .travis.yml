language: java
jdk:
- oraclejdk7
cache:
  directories:
    - $HOME/.gradle
    - .gradle
sudo: false
install: true
#branches:
#  only:
#    - master
#    - build

before_install:
  - cat .git/config
  - sed -i 's/git:/https:/g' .git/config
  - cat .git/config
#script:
#  - gradle build publishGhPages
#notifications:
# irc:
#  channels:
#   - "irc.esper.net#Disconsented "
#  template:
#   - "%{build_number} (%{branch} - %{commit}: %{commit_message} : %{author}): %{message} (%{build_url}) "